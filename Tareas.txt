login vm -> azureuser
la contraseña es o "1234"
He puesto la clase usuario y usuarioEntity que todos los campos puedan ser nulos, es posible que esto haya que quitarlo

Para conectar con mysql usamos las siguientes credenciales: uremoto, contraseña -> 1234

Tareas
---------

Comprobar y arreglar si cuando haces logout desde profile, todos los viewmodel(UiState,listasy datos) vuelven a estar vacios.
Meter la lazycolumn de rutinas y de añadir rutina en un componente para no repetir codigo
Meter un boton en ver rutinas donde puedes quitar la seleccion de una rutina, es decir, que no salga con el color amarillo fuerte y se quite la papelera de borrar.
Añadir la autenticacion login con retrofit, todo está al final del pdf de retrofit en aules
Mejorar el ExposedDropDownMenuBox de edad sexo somatotipo para que solo sea un componente y lo llave al mismo,
no repetir codigo, se solucionaria pasandole el array por parametro y creando un label para el "titulo" y cambiarle
el padding bottom para que haya mas espacio entre uno y otro 

Ampliaciones
-------------





/*RutinamaquinaJpaController dao2 = new RutinamaquinaJpaController(emf);
            Query query = em.createNamedQuery("Rutinamaquina.findByRutinaidAndDia");
            query.setParameter("rutinaid", rutinaid);
            List<RutinamaquinaPK> rutinamaquinapklist = query.getResultList();*/
            /*for(RutinamaquinaPK rtp : rutinamaquinapklist)
                {
                    dao2.destroy(rtp);
                }*/
            
